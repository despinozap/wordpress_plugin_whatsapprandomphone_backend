{
    "name": "app",
    "version": "1.0.0",
    "description": "",
    "main": "index.js",
    "scripts": {
        "dev": "NODE_ENV=development nodemon index.js",
        "lint": "eslint ./",
        "start": "NODE_ENV=production node index.js",
        "deploy": "vercel",
        "test": "NODE_ENV=test jest --verbose --silent",
        "test:verbose": "NODE_ENV=test jest --verbose",
        "test:watch": "NODE_ENV=test jest --verbose --silent --watchAll"
    },
    "keywords": [],
    "author": "",
    "license": "ISC",
    "devDependencies": {
        "eslint": "^8.21.0",
        "jest": "^28.1.3",
        "nodemon": "^2.0.19",
        "supertest": "^6.2.4"
    },
    "dependencies": {
        "cors": "^2.8.5",
        "express": "^4.18.1"
    },
    "jest": {
        "testEnvironment": "node"
    }
}